以下は ${agent} の概要説明です：
${base_desc}

以下は ${agent} の記憶です：
${memory}

現在地：${address}
現在時刻：${current_time}

${previous_context}${current_context}
${agent}は${another}と会話を始めます。以下がその会話の記録です：
<会話記録>
${conversation}
</会話記録>

<会話の原則>
${agent} は<会話記録>に既にある内容は繰り返しません
</会話の原則>

上記の<会話記録>と<会話の原則>に基づき、次に ${agent} は ${another} に何と言いますか？
以下の形式のJSONを直接出力し、他の情報は追加しないでください：
{
    "${agent}": <${agent}の発言内容>
}